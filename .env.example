# Database (Postgres) settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=auto
POSTGRES_PORT=5433
POSTGRES_HOST=localhost

# App settings
NODE_ENV=development
PORT=3000

# Connection string used by Prisma / app
# Replace values if you change the DB credentials above
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/auto?schema=public
JWT_ACCESS_SECRET=secret
